import 'package:flutter/material.dart';

class ThemeColor {
  static Color inactiveLight = Colors.white.withOpacity(0.3);
  static Color pseudoLight = Colors.white.withOpacity(0.8);
  static Color light = Colors.white;
  static Color inactiveDark = Colors.black.withOpacity(0.3);
  static Color pseudoDark = Colors.black.withOpacity(0.8);
  static Color dark = Colors.black;
  static Color transparent = Colors.transparent;
}
